============
Installation
============

At the command line::

    $ pip install language-helper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv language-helper
    $ pip install language-helper
